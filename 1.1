import java.util.Scanner;

class G {
    public static void main(String[] args) {
        Scanner s = new Scanner(System.in);
        System.out.print(gcd(s.nextInt(), s.nextInt()));
    }
    
    static int gcd(int a, int b) {
        return b == 0 ? a : gcd(b, a % b);
    }
}
